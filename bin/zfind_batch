#!/usr/bin/env python
from pbs import queue
queue = queue()
nodes = 12
walltime = '336:00:00'
queue.create(label='zfind',nodes=nodes,walltime=walltime)


plates = [8123]
mjds = [56931]

for i,plate in enumerate(plates): queue.append("zfind -p %i -m %i " % (plate,mjds[i]))
queue.commit(hard=True,submit=True)
